import { colors, spacing } from "./tokens";

export const theme = {
  colors,
  spacing,
  radius: {
    sm: 8,
    md: 14,
    lg: 20,
  },
  shadow: {
    card: {
      shadowColor: "#000",
      shadowOffset: { width: 0, height: 8 },
      shadowOpacity: 0.08,
      shadowRadius: 16,
      elevation: 3,
    },
  },
};
